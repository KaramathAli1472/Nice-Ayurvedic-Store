name: nice_ayurvedic_app
description: Nona Ayurvedic Store App
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  cloud_firestore: ^4.13.6
  firebase_storage: ^11.6.0 # ✅ VERSION CHANGED (Compatible)
  cached_network_image: ^3.3.0 # ✅ VERSION CHANGED (Compatible)
  google_fonts: ^5.1.0     # ✅ VERSION CHANGED (Compatible)
  provider: ^6.0.5         # ✅ VERSION CHANGED (Compatible)
  intl: ^0.18.1            # ✅ VERSION CHANGED (Compatible)
  cupertino_icons: ^1.0.2
  firebase_messaging: ^14.7.5
  # flutter_local_notifications: ^14.1.1
  
  flutter_otp_text_field: ^1.1.0
  geolocator: ^10.0.0
  geocoding: ^2.1.1
  firebase_auth: ^4.15.3
  google_sign_in: ^6.1.6
  
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
